---
description: Markdown on disk for chapters (author sees Word-like editor)
globs: current manuscript/**/*.md,story planning/**/*.md,diary enteries/**/*.md,general references/**/*.md
alwaysApply: false
---

# Manuscript on disk

Author sees Office Viewer. On disk: standard markdown for backup/AI.

- One `#` title per chapter; scenes with `##` or `---`.  
- Names: `01-slug.md`; keep `00-table-of-contents.md` in sync.  
- No invented facts; confirm before big cuts.  
- To the author: toolbar language only — never markdown syntax.
